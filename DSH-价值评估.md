# DeepSeek Harness 个人系统 · 价值评估

> 评估对象：你在 `dsh-core-stopgap` 分支上的个人 fork（基于 `v0.1.1-rc.2` + ~37 个本地提交）。
> 视角：**价值层面**，而非工程质量层面。工程质量你已有 `DSH-评估报告.md`（上游最新版 7.0/10）与 `DSH-fork-增量-审报告.md`（你的增量）。本报告回答的是：**这套个人系统对"你"到底值多少、价值从哪里来、哪里在贬值、接下来怎么保住价值。**

---

## 0. 一句话结论（TL;DR）

**底子是真的很值钱，但"你的价值"现在被包在一个正在贬值的壳里。**

- **值钱的部分**：DeepSeek Harness 本身是一个工程化程度接近同类最佳、且"万物皆插件 + 无特权核心"的开放性 agent 框架——纯开源零授权成本，可靠性 8.7 / 扩展性 8.0。等于免费拿到一套你能**完全摸透、任意弯曲**的 agent 基础设施，这是 Claude Code / Codex 这类黑盒工具给不了的控制力。
- **正在贬值的部分**：你的个人产出（加固 + 工具 + 思路）是以**一个落后上游 1313 个提交的长期 fork** 为载体的。fork 本身不是价值，是**包袱**——它把你辛辛苦苦做的正确加固，绑定在一个旧基底上，每一天都在累积合并成本。

**So what**：**价值在"能力 + 你的行为护栏"，不在 fork 本身。** 把工作从"维护一个分叉"改造成"在最新上游之上用 bundle/profile + AGENTS.md + skill 重新表达你的护栏"，价值就能从"会贬值"转向"可持续、可迁移"。当它作为日常主力工具使用时，ROI 是高的；当它作为一个需要长期维护的 fork 时，ROI 在衰减。

---

## 1. 你手里到底有什么（资产清单）

| 资产 | 规模/证据 | 对"你"的价值 |
|---|---|---|
| 能力广度 | 约 48+ 模型可见工具 / 52 能力域 / 7 入口 / 6 子代理 provider / 5 沙箱后端 / 254 包 / 116 配置项 | 一套能覆盖 web 检索、浏览器、shell、子代理、工作流、持久会话、编排的完整工具箱 |
| 工程可信度 | 可靠性 8.7（崩溃安全写入、模型可见⟺可记录 THEOREM 级测试、per-file 100% 覆盖门禁）；全量回归 861 文件 / 14650 tests 通过（39 失败仅 PTY 环境假象） | 一个可以"放心把钱压上去"的底层——bug 少、可恢复、可断言 |
| 架构开放性 | "万物皆插件"，agent-loop 本身是插件，无特权核心可打补丁 | 你能做到"换个 provider / 改行为 / 挂自己的钩子"，而不只是调用方 |
| 你的个人护城河 | 37 提交 / 183 文件 / +8339+ 行；19 项 P0 已修并验证；新增 `tool-browser`(D3)、`tool-service`(D4)、`apply_patch`(F-3)、`observed-read` 修正、SSRF 回归修复 | 一套按"个人自用"重排过优先级的**行为护栏**：防我机器被整坏、防工作流被卡、防数据丢失 |
| 你的方法论资产 | AGENTS.md 自动加载、skills 体系、会话回放 / session-query 审查、goal/plan 管长目标 | 这才是**最高价值的杠杆**——让 agent 按你的标准干活，这是复利 |
| 规模/复杂度成本 | 4.5GB 工作区（1.6G .eval / 1.3G node_modules / 1.3G .pnpm-store）；1492 个 Agent Notes；516 个 package.json | 一面是能力深度，一面是你个人维护的认知负担 |

---

## 2. 价值从哪里来（价值驱动因素）

按对"个人自用"的重要度排序：

1. **时间杠杆（最高）**：你一天的流程是"派 agent 干活 → 它改代码 → 你 review"。这套系统的价值 = 你省下的时间 − 你维护它的时间。只要你**真的天天用**，它是净正贡献。
2. **可控性/可摸透**：你明确把"能摸透、工作流越用越顺"列为标准。`everything-is-a-plugin` + 无特权核心 + 128 个机器校验的文档目录，让你能**审计、改造、打补丁**，而不用接受黑盒。这是它相对商业工具的**结构性优势**。
3. **可靠性兜底**：崩溃安全写入、模型可见⟺可记录不变量、fail-closed 读取——对"agent 会犯错"的场景是刚需。你担心的不是"别人攻击我"，而是"agent 把我机器/数据搞坏了"。这套可靠性正是对冲这个的。
4. **你的护栏（差异化）**：SSRF 回归修复、tool-browser 封文件读/路径穿越、tool-service 进程组杀/有界 tail/默认 workdir、MCP 超时、流终止预算、事务化 step 准入——这些是为"个人自用"定制的、上游没有或该修没修的。**它们是你增量价值的本体。**

---

## 3. 成本与价值侵蚀（什么在偷走价值）

| 侵蚀因素 | 证据 | 影响 |
|---|---|---|
| **fork 分叉（最重）** | 落后 `origin/master` **1313 提交**（评估时 1079），领先 37；merge-base 在 `v0.1.1-rc.2`（8-21） | 你的加固跑在一个比最新版旧 ~9 天、又落后 1.3k 提交的基底上。上游的可靠性/安全修复你收不到，且随天数累积合并冲突。**这是当前最大的价值漏洞。** |
| **pre-release 动荡** | `v0.1.2-alpha.1`，"THERE WILL BE COMPATIBILITY-BREAKING CHANGES"；`SESSION_FORMAT_VERSION`=0、无跨版本升级器 | 配置、会话格式、事件 schema 会变。你把"自定义"立在会变的地基上，每次升级都要重练。 |
| **复杂度** | 116 配置项 / 254 包 / 深度 profile/bundle/patch/preset 分层 | 单靠你一个人摸透，认知成本高；踩坑（如 `[::1]` 方括号、`!!js` 配置即代码）会让"维护时间"吞噬"使用收益"。 |
| **安全姿态 4.5/10** | 沙箱只约束"写"，读/网不设防，无机密性；上游自述"不可视为安全/生产就绪" | 对"个人自用、无外部攻击者"不是硬伤（你的 P0-3/P0-2 已把"别整坏自己"补上），但意味着**这东西不能接内网/敏感数据/不能暴露到网络**。这是使用纪律，不是可修缺陷。 |
| **自增安全短板** | 你的 `tool-browser`/`tool-service` 曾把"原版安全短板"带回来（SSRF/任意读/绕沙箱命令），且 SSRF 层一度既回归又没真堵（IPv4-mapped/NAT64 可绕过） | 增量审计已揪出并多数修复；但它提醒：**每次自己加工具，都要过一遍"是否复活了原版防线"这一关**。这是维护这类自定义系统的固有税。 |

---

## 4. 价值评估计分卡（针对"个人自用"重排）

> 这不是给上游打分的重复（你已有 7.0/10）。这是对"这套 fork 作为**你的个人工具**"的价值评估。

| 维度 | 得分 | 说明 |
|---|---|---|
| **基础能力价值** | **8.5 / 10** | 工具箱完整、文档标杆级、单命令开箱；这是你免费拿到的强地基 |
| **个人增量价值** | **7.5 / 10** | 护栏方向完全正确（防整坏/防卡/防丢数据），且已用真实 vitest 验证；但集中在"防错"而非"提效"，天花板受限 |
| **可靠性/可依赖性** | **8.5 / 10** | 崩溃安全、可断言、可恢复；全量回归健康 |
| **维护成本** | **3.5 / 10** | fork 落后 1313 提交 + pre-release 动荡 + 116 配置项 = 单人维护负担偏重，是负分项 |
| **可持续性** | **4.0 / 10** | 只要"fork 是载体"这一条不改，价值随时间衰减；一旦改成"bundle+skill+AGENTS.md 挂最新上游"，可持续性大幅回升 |
| **风险敞口** | **中高** | 安全姿态 4.5 + 前向兼容无承诺；适合"个人无敏感数据 + 不暴露网络"场景 |

**综合判断**：**基础能力高、个人增量中等偏上、但承载方式（fork）正在损害它。** 当前形态下算 6.5/10；把"承载方式"改掉后可到 8/10。

---

## 5. 诚实的价值判断（这里我直接说）

1. **这套系统值不值得做？值得，但要换一种做法。**
   - 值得的理由：你拿到的是**同类最佳的可弯曲 agent 框架**，且它的强项（可靠 8.7 / 扩展 8.0）恰好对冲"agent 会犯错"这个你最在意的日常风险。你能摸透、能改造，这对一个想深度掌控自己开发流程的人是稀缺的。
   - 要换的原因不是系统不好，而是**你现在的"价值载体"选错了**：一个落后 1313 提交的长期 fork。

2. **fork 是包袱，不是资产。** 37 个提交的价值（护栏）应该被**提取成可重放的东西**，而不是被"卡死在一个旧分支上"。harness 天生支持这个：`bundle`（patch 层叠加）、`cordis.patch.yml`、profile 叠放、skill、AGENTS.md。你的护栏全部可以表达成**一组 patch + 一个 profile + 若干 skill + 每个项目的 AGENTS.md**，然后随时 rebase/重放到最新上游。

3. **安全性 4.5 对你的场景不是否决项，是纪律项。** 你不是"防外部攻击者"，而是"别被 agent 整坏 + 别把 secret 拉进上下文/日志"（你已经在计划里写对了）。做到"不用内网/敏感数据/不暴露网络 + 项目里不放真 secret + `file://` 已封"就够。别为不存在的多租户威胁过度投入——那是上游的主战场。

4. **最高价值不在 fork，在你已在做的那件事上：AGENTS.md + skill + 让 agent 按你的标准干活。** 这是复利，是你和"开箱用 Claude Code"的人拉开差距的地方。fork 再修也只解决"别出错"，这个解决"出活更快、更强"。

---

## 6. 建议行动（按杠杆从大到小）

1. **（最关键）换承载方式：把护栏从 fork 里提取出来。**
   - 把这 37 个提交的改动改写成：一个 **personal-hardening 的 cordis bundle/profile**（把 P0-1/2/3、MCP 超时、流终止预算、事务化 step 准入等作为 patch 层挂上去），一个 **build/test/commit 的 skill**，若干你真实项目的 **AGENTS.md**。
   - 然后**把 fork 重放到最新 `origin/master`（v0.1.2-alpha.1）**，或直接基于最新版 + bundle 重新来过。这样你的增量价值**可迁移、可持续**，不再被 1313 提交的分叉拖死。

2. **先对齐最新版再谈加固。** 你的 P11（单调 guard）与 P1-3（FrameQueue resync）都需要 Web 客户端配合、且你当前环境验证不了 Web 端——这类正好是"在最新版上做"最划算。落后 1313 提交再自修，等于在旧地基上打补丁。

3. **锁定一个版本，别带数据跨版本。** `SESSION_FORMAT_VERSION` 无升级链，做产品阶段把会话数据锁在当前版本，别中途升级。

4. **使用纪律固化为文档。** 把"不接内网/敏感数据、不暴露网络、项目里不放真 secret、`file://` 已封"写进你的 AGENTS.md/skill，让 agent 自己遵守。

5. **用你真机验证 Web 端。** P1-3 的 `session/resync` 帧 + 客户端配合，你在这套环境的 Web 端**无法端到端验证**——预留一次真机冒烟，别让它悬而未决。

6. **别为多租户安全过度投入。** 安全 4.5 是上游的短板、也是"未审计实验软件"的固有姿态；你把它当"个人自用纪律"处理就是最优，不必追着补到 8 分。

---

## 7. "值不值得"的终极判据

这套系统对你是净赚还是净亏，只取决于一件事——**你是不是真的天天用它。**

- **是**：价值 = 每天省的时间（工作流杠杆 + AGENTS.md + skill 复利）远大于维护成本（对齐最新版、写 bundle/skill）。**继续，但改承载方式。**
- **否**（只是偶尔尝鲜/学习）：那 fork 的维护成本会盖过收益。**先把护栏提取成 bundle/skill 存档，别让它成为持续负债；需要时再拿出来重放到最新版。**

> 一句收尾：**你手里的东西本身极值钱，但"怎么持有它"正在决定它值多少。** 把它从"一个落后的个人分支"改造成"一个挂在最新上游上的能力层 + 你每个项目里的 AGENTS.md + 一套 skill"，你的增量价值就从会贬值变成复利。

*本报告为静态/证据回顾式评估（基于 git 历史、你的既有报告、目录与能力目录），未重新跑构建/测试套件；工程质量与修复状态以你已有的 `DSH-评估报告.md` / `DSH-fork-增量-审报告.md` / `DSH-fork-修复-演进计划.md` 为准。*
